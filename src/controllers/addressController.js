const createNewAdress = async (userId, cityId, subregionId, countryId) => {
  try {
  } catch (error) {
    console.log(error);
    throw new Error("somthing went wrong");
  }
};

module.exports = {
  createNewAdress,
};
