const Subregion = require("../database/model/Subregion.js");

const getAllSubregions = (req, res) => {
  try {
  } catch (error) {
    return res.status(500).json({ res: "SOMETHING WENT Wrong" });
  }
};
